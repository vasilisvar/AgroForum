# AgroForum project instructions

## Pending Git workflow item

- On the next push, also revisit the existing `feature/day6-green-design` branch and rename it to a clearer professional name.
- Ask the user to confirm the exact replacement branch name before changing local or remote refs.
- After confirmation, rename the local branch and its remote counterpart, set the new upstream, and verify the replacement branch exists before removing the old remote ref.
- Keep this item pending until the branch rename has been completed and verified.

Do not rename or push the Day 6 branch merely because this instruction was read; perform it only as part of the next user-authorized push.
